global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'kong'
    static_configs:
      - targets: ['localhost:8100'] # Kong metrics endpoint
    metrics_path: /metrics